# Code as Worlds 复现与数据展示项目：数据留存约定

本文件适用于本项目的代码代理及人工开发。负责人：刘子锴。目标是让 Benchmark 评测、数据构建、训练和展示页面使用同一套可追溯记录；每个结论能下钻到案例、输入、计算依据及运行版本。按实际完成的节点逐步扩充，未运行的模型或未实现的阶段标为“待评测 / 待构建”，不得补填推测结果。

## 1. 通用规则

- 每次处理都留存 **输入引用、机器可读中间产物、最终产物、质量判定、运行配置**。有视频或图片输出时，另生成网页可直接查看的预览；预览不能替代原始数值或标注。
- 不覆盖原始数据、仿真原视频、失败产物或旧轮次；新结果写入新的 `run_id`、`iteration_id` 或数据版本。原始模型回答与解析结果分开保存。
- 给原始样本分配稳定 `case_id`；同一样本的各处理阶段沿用它。一个案例可产生多道题，分别用 `qa_id` 标识；复制、派生或混合成新样本时用新的 `case_id` 并记录 `parent_case_id`。单次执行、单次评测、训练实验分别使用 `run_id`、`evaluation_run_id`、`experiment_id`，不要将这些 ID 混用。
- 所有文件使用相对项目根目录的路径或稳定对象存储 URI；记录文件格式、大小和校验和。大视频、帧、mesh、checkpoint 存为文件，清单中保存引用，不内嵌进 JSON。单位、坐标系、时间基准、FPS 和缺失值规则须显式记录。
- 记录 `dataset`、原始 ID、`split`、`source_type`、任务类别、生成/评测代码 commit、配置文件、模型及权重版本、创建时间、执行状态和失败原因。训练与评测案例按源样本隔离，派生案例继承隔离边界；发现交叉污染时停止使用相关结果。
- 对每条数值答案记录来源类别（原标注、像素计算、仿真状态、拟合估计等）、公式、参与计算的原始字段和单位。仿真状态只能作为该仿真场景的标签；若对应真实视频，另记尺度与相机标定、拟合方法及误差，不能称为真实视频的精确真值。
- 每个阶段保留 `status`（`pending`、`success`、`failed`、`rejected` 等）、机器可读 `failure_reason`、必要的人工复核结论。分别统计输入数、处理数、成功数、剔除数和原因；通过率需说明分母。

最小公共记录示例（具体字段可扩充；不适用字段不必伪造）：

```json
{
  "case_id": "quantiphy_val_000123",
  "source_id": "original-id",
  "dataset": "QuantiPhy",
  "split": "val",
  "source_type": "real_video",
  "task_type": "velocity",
  "run_id": "run_20260923_001",
  "code_commit": "<git-sha>",
  "config_path": "configs/example.yaml",
  "status": "success",
  "failure_reason": null,
  "created_at": "2026-09-23T00:00:00Z"
}
```

## 2. Benchmark 与模型逐案例评测

### 2.1 Benchmark 清单

每个 Benchmark 保存名称、版本、官方仓库 commit、数据来源与许可证、split、原始样本量、实际评测样本量、任务分类、指标名称及实现版本、数据 URI。以 QuantiPhy 为起点，其他 Benchmark 加入时写明能力覆盖、规模、划分、指标与采用理由。不要预设样本数或指标取值，实际值从锁定的版本核对。

### 2.2 Case 清单

每个案例保存：`case_id`、原始 index/ID、媒体路径、时长、FPS、分辨率、原问题、先验、标准答案与单位、任务类别和物理量类别。对于采样帧推理，保存实际输入模型的帧文件或可重建的引用、frame index、timestamp、采样规则及输入分辨率；必要时生成采样帧拼图。

### 2.3 每次模型推理

对每个 `case_id × model_id × evaluation_run_id` 保存：精确模型 ID、权重/checkpoint/revision、推理后端与版本、GPU、dtype、量化、temperature、top_p、max tokens、完整 system/user prompt 或模板及其版本、视频采样配置；原始回答、解析值、单位、解析器版本、解析状态、得分、错误类型、耗时、用量、调用错误和重试次数。重试应有独立 attempt 记录及最终采用哪次的规则。

展示页应能按 Benchmark、模型、类别查看指标、样本数与覆盖率，并下钻到**全部**案例，查看实际输入帧、标准答案、各模型原文、解析结果与评分。缺失、调用失败和解析失败分开统计；评测设置不同的模型需注明不可直接比较之处。优先保留定位、时间、尺度、运动、单位和格式等错误标签及案例证据。

## 3. 数据构建六个节点

### 节点 1：像素空间测量

- **原始标注：**图像/视频引用、原始 annotation ID、描述、类别、bbox/mask；跟踪数据的逐帧 bbox、可见性和缺失标记。
- **计算依据：**图像宽高、坐标约定；逐帧 frame、timestamp、bbox、中心、尺寸、面积；所用帧区间、`dx/dy`、位移、像素速度和轨迹。存原值与计算公式，避免仅保存最后答案。
- **问答成品：**独立 `qa_id`、问题、答案、单位、答案类型、目标、引用帧、计算方法、操作数、来源标注 ID。
- **质量与预览：**bbox 越界、缺帧、不可见、异常跳变、自动校验、人工抽查及拒绝原因；原图/视频叠加 bbox、中心与轨迹，按数据集和问题类型统计。

### 节点 2：文本 → 可执行世界

- **输入：**原始文本、结构化场景要求、物体和物理约束；保留人工调整前后的要求。
- **场景：**物体几何、尺寸、质量、位置、姿态、初速度、材料、摩擦、恢复系数、约束；重力、求解器、步长、相机、光照与时长。保存 scene JSON、MJCF/XML 或代码以及所有资产引用。
- **执行：**逐时间戳位置、姿态、速度、角速度、接触/力等可获得状态；原始渲染视频、缩略图和状态/轨迹曲线。标记“轨迹驱动动画”或“物理参数推演仿真”，两者分开评价。
- **验收：**逐项列出要求、期望、实际、容差和通过判定；人工改动留存修改前后场景、原因与操作者。

### 节点 3：真实视频 → 初始可执行世界

每一步保留机器产物、模型/配置及预览，使失败可定位：

1. 原视频元数据、选取的帧与时间戳。
2. 分割：逐帧 mask（RLE/PNG 等）、object ID、bbox、置信度及叠加视频。
3. 跟踪：稳定 track ID、逐帧 bbox/mask/中心、可见性、置信度及叠加视频。
4. 深度：原始数值、显示用彩图、尺度性质（绝对/相对）及模型版本。
5. 相机：内参、逐帧位姿/外参、焦距、坐标和单位约定。
6. 几何：mesh/point cloud、尺度、原点、坐标系及预览。
7. 运动：2D 实测轨迹与 3D 重建轨迹分别保存，记录帧、时间、位置、姿态、速度及拟合依据。
8. 初始世界：scene、MJCF/XML、脚本、资产和仿真配置；模拟状态、视频与原视频并排预览，记录物体、轨迹、几何及相机的偏差。

某一步未运行时写 `pending` 与原因，不能把缺失结果描述成已生成。

### 节点 4：迭代修正与验证

每轮使用独立 `iteration_id` 并关联上一轮：修正前场景和视频、原评分；诊断文字及结构化错误类型；参数/代码修改的旧值、新值、原因与补丁；修正后场景、状态、视频和指标。记录轨迹误差、中心误差、silhouette IoU、深度误差或碰撞时刻误差等**实际使用**的指标定义、对齐方法、单位和数值；保留退化/无效轮次。最终记录采纳/拒绝、停止原因（收敛、无改善、无效场景、达到轮数等），按固定规则判断收益。

### 节点 5：仿真视频写实化

分别保留原仿真视频、写实输出、对比预览。记录条件帧、prompt、生成模型精确版本、seed、配置、任务 ID、耗时和失败信息；重新测得的写实视频轨迹与原轨迹、运动误差、身份/数量/形状/时序/相机一致性检查。保存漂移、变形、消失、多物体等拒绝标签及 `keep/reject` 结论；统计产出和剔除数。写实化不能覆盖原仿真数据，运动标签须重新核验。

### 节点 6：物理问答成品

每条 QA 保存 `qa_id`、`case_id`、video/scene ID、来源类型、目标物体、时间区间、问题、任务及物理量、结构化先验、数值答案、单位、精度、展示答案、答案来源、公式与输入变量。附视频、可见性、时间同步与计算一致性检查；保留失败样本与原因。发布数据版本时固定清单、划分、数量、类别分布、通过率及筛选规则；训练只引用固定版本。

## 4. 训练与训练前后验证

每次训练用独立 `experiment_id` 保存基座模型/checkpoint、训练数据版本与 manifest、train/val 划分、样本数和类别配比、完整配置 YAML（学习率、批大小、累积、轮数、优化器、scheduler、LoRA、SFT/GRPO 与奖励定义、KL、精度、GPU、seed 等实际使用项）、代码 commit、环境、日志、checkpoint、tokenizer/adapter 及结束状态。日志记录实际可获得的 step、loss、lr、grad norm、reward 分项、KL 和验证结果；不编造不存在的指标。

训练前后复用同一 Benchmark Case、ModelRun、Evaluation 结构及固定指标，记录评测代码和配置。网页按相同样本集合计算总体、分类、改善和退化；这些标签由两次评测动态计算，指标变更后重新计算。明确论文方法中实际复现部分、修改设置与替代实现。

## 5. 网站、索引与版本

第一版仅需四个核心实体：`Case`（来源与题目）、`Artifact`（阶段文件及预览）、`ModelRun`（模型单次推理）、`Evaluation`（评测集合及指标）。后续增加 `QA`、`Iteration`、`Experiment` 等记录；各实体使用稳定 ID 和关联字段，不靠文件名猜关联关系。

页面至少提供：评测总览、全部案例的模型对比、类别与错误筛选、六节点的案例链路、各轮修正记录、数据版本统计、训练前后对比。每次汇报固定保留结果快照、代码 commit、数据/模型版本、配置、代表性成功与失败案例、处理量/通过量/剔除量、人工修改量和主要计算开销。

推荐目录（可调整目录名，但 ID、版本和追溯关系不可丢）：

```text
data/
  benchmarks/{benchmark_version}/
  raw/{case_id}/
  pixel_measurement/{case_id}/{run_id}/
  world_generation/{case_id}/{run_id}/
  reconstruction/{case_id}/{run_id}/
  refinement/{case_id}/{run_id}/iter_00/, iter_01/, ...
  realism/{case_id}/{run_id}/
  qa/{dataset_version}/manifest.json, records.jsonl
  training/{experiment_id}/config.yaml, dataset_manifest.json, log.jsonl, checkpoints/
  evaluation/{evaluation_run_id}/config.json, predictions.jsonl, metrics.json
```

各阶段的产物清单应包含 `artifact_id`、`case_id`、`run_id`、`stage`、`kind`、`uri`、`format`、`checksum`、`status`、`created_at`、`producer`、`config_path`、`parent_artifact_ids` 和可选的 `preview_uri`。不要为网页另外维护一份无法回溯的“真值”；网页从固定版本的清单和结果记录读取。

## 6. 当前推进顺序与完成标准

1. 锁定 QuantiPhy 数据/指标版本，跑通至少一个真实模型并建立案例索引；展示原视频、采样帧、原回答、解析结果和评分。
2. 增加可运行的对照模型；未接入的模型标明原因，形成分类统计和有证据的错误案例。
3. 依据评测缺口选择首批数据类别，先做一个从输入到 QA 的小闭环，再扩展六个节点。
4. 只有数据通过质量检查且版本固定后再用于训练；在未接触的固定测试集上比较训练前后。

一个节点称为“完成”，须能从页面打开真实案例，访问其机器产物与预览，核对计算或判定依据，识别失败案例，并查到数据、代码、模型及配置版本。论文未公开的步骤需记录具体假设或替代实现，不标为官方复现。
